## 1. Context API IN REACT

```
1. To pass the props from top level to the deep bottom in the dom.
2. This helps to resolve the problem of prop drilling.
3. Mainly usefull for auth, themes and something which is common all over the react app.
```
### 2. UseReducer
```
1. useReducer is a hook that is used for state management.

2.It is alternative to useState.

Q.What's the difference ?
A. useState is built uisng useReducer.

Q. When to useReduer vs useState ?
A. 
```

# What is a useReducer ?
```
1.Has 2 parameters => (reducer, initilaState)
newState => reducer(currentSate, action)

2.useReducer => returns pair of values [newState, dispatch]

```
